import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'
const cors={'Access-Control-Allow-Origin':'https://alin653.github.io','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS'}
const json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{...cors,'Content-Type':'application/json'}})
async function paypalAccessToken(){const id=Deno.env.get('PAYPAL_CLIENT_ID'),secret=Deno.env.get('PAYPAL_CLIENT_SECRET');if(!id||!secret)throw new Error('PayPal nu este configurat pe server.');const r=await fetch('https://api-m.paypal.com/v1/oauth2/token',{method:'POST',headers:{Authorization:`Basic ${btoa(`${id}:${secret}`)}`,'Content-Type':'application/x-www-form-urlencoded'},body:'grant_type=client_credentials'});const d=await r.json().catch(()=>({}));if(!r.ok||!d?.access_token)throw new Error('Nu am putut autentifica PayPal.');return d.access_token as string}
async function getOrder(id:string,t:string){const r=await fetch(`https://api-m.paypal.com/v2/checkout/orders/${encodeURIComponent(id)}`,{headers:{Authorization:`Bearer ${t}`}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.message||'Nu am putut verifica plata PayPal.');return d}
async function captureOrder(id:string,t:string){const r=await fetch(`https://api-m.paypal.com/v2/checkout/orders/${encodeURIComponent(id)}/capture`,{method:'POST',headers:{Authorization:`Bearer ${t}`,'Content-Type':'application/json','PayPal-Request-Id':`capture-${id}`},body:'{}'});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.message||'Nu am putut confirma plata PayPal.');return d}
function parseCustomId(v:unknown){const p=String(v||'').split('|');if(p.length!==3)throw new Error('Referință PayPal invalidă.');return{purpose:p[0],jobId:p[1],userId:p[2]}}
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{const auth=req.headers.get('Authorization')||'';if(!auth.startsWith('Bearer '))throw new Error('Autentificare necesară.');const accessToken=auth.slice(7);const {order_id}=await req.json();if(!order_id)throw new Error('Comandă PayPal invalidă.');const url=Deno.env.get('SUPABASE_URL')!,service=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,admin=createClient(url,service);const {data:{user},error:userErr}=await admin.auth.getUser(accessToken);if(userErr||!user)throw new Error('Sesiune invalidă.');const paypalToken=await paypalAccessToken();let order=await getOrder(String(order_id),paypalToken);const unit=order?.purchase_units?.[0];const ref=parseCustomId(unit?.custom_id);if(ref.userId!==user.id)throw new Error('Plata nu aparține acestui cont.');if(order.status!=='COMPLETED')order=await captureOrder(String(order_id),paypalToken);if(order.status!=='COMPLETED')throw new Error('Plata PayPal nu este finalizată.');const completedUnit=order?.purchase_units?.[0]||unit;const paidValue=Number(completedUnit?.payments?.captures?.[0]?.amount?.value||completedUnit?.amount?.value||0);const currency=String(completedUnit?.payments?.captures?.[0]?.amount?.currency_code||completedUnit?.amount?.currency_code||'');if(ref.purpose==='job_publish'){const {data:job,error:jobErr}=await admin.from('jobs').select('id,client_id,publish_fee,payment_status').eq('id',ref.jobId).single();if(jobErr||!job||job.client_id!==user.id)throw new Error('Lucrarea nu a fost găsită.');const expectedEur=Number((Number(job.publish_fee||0)/5).toFixed(2));if(currency!=='EUR'||Math.abs(expectedEur-paidValue)>0.001)throw new Error('Suma PayPal nu corespunde taxei de publicare.');if(job.payment_status!=='paid'){const {error}=await admin.from('jobs').update({payment_status:'paid'}).eq('id',ref.jobId).eq('client_id',user.id);if(error)throw new Error('Plata a fost confirmată, dar lucrarea nu a putut fi activată.')}return json({ok:true,purpose:'job_publish',job_id:ref.jobId})}if(ref.purpose==='job_unlock'){if(currency!=='RON')throw new Error('Monedă PayPal invalidă.');const {data:job,error:jobErr}=await admin.from('jobs').select('id,status,unlock_fee,max_unlocks,payment_status').eq('id',ref.jobId).single();if(jobErr||!job||job.status!=='open'||job.payment_status!=='paid')throw new Error('Lucrarea nu mai este disponibilă.');if(Math.abs(Number(job.unlock_fee||0)-paidValue)>0.001)throw new Error('Suma PayPal nu corespunde taxei de acces.');const {data:existing}=await admin.from('job_unlocks').select('id,status').eq('job_id',ref.jobId).eq('worker_id',user.id).maybeSingle();if(existing?.status==='paid')return json({ok:true,purpose:'job_unlock',job_id:ref.jobId,already:true});const {count}=await admin.from('job_unlocks').select('id',{count:'exact',head:true}).eq('job_id',ref.jobId).eq('status','paid');if((count||0)>=Number(job.max_unlocks||6))throw new Error('Lucrarea a atins limita de meseriași.');const payload={job_id:ref.jobId,worker_id:user.id,amount:paidValue,status:'paid',provider_reference:String(order_id),paid_at:new Date().toISOString()};const result=existing?await admin.from('job_unlocks').update(payload).eq('id',existing.id):await admin.from('job_unlocks').insert(payload);if(result.error)throw new Error('Plata a fost confirmată, dar accesul nu a putut fi salvat.');return json({ok:true,purpose:'job_unlock',job_id:ref.jobId})}throw new Error('Tip de plată PayPal necunoscut.')}catch(e){return json({error:e instanceof Error?e.message:'Eroare'},200)}})